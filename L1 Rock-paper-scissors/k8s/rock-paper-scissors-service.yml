apiVersion: v1
kind: Service
metadata:
  name: rock-paper-scissors-service
  namespace: rps-ns
spec:
  selector:
    app: rps-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30130
  type: NodePort