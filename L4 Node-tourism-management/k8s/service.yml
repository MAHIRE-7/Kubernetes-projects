kind: Service 
apiVersion: v1
metadata:
  name: tm-service 
  namespace: tm-ns

spec:
  selector:
    app: tm-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: NodePort
